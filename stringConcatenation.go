//This code shows how to concatenate strings to create a full name from the first and last names

package main

import "fmt"

func main() {
    firstName := "John"
    lastName := "Doe"
    fullName := firstName + " " + lastName
    fmt.Println("Full Name:", fullName)
}
